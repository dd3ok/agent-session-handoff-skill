interface:
  display_name: "New Session Handoff"
  short_description: "Create or resume safe agent session handoffs"
  default_prompt: "Use $new-session-handoff to create HANDOFF.md, provide a prompt-only handoff, or read HANDOFF.md and continue the work."

policy:
  allow_implicit_invocation: true
